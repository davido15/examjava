public class ProductImpl extends Product {
    public ProductImpl(String productID) {
        super(productID);
    }

    @Override
    public double getPrice() {
        return 0;
    }
}
